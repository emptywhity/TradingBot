# Public data does not require keys, but you can add a webhook for alerts.
VITE_DISCORD_WEBHOOK_URL=
#
# Note: This demo uses public Binance REST endpoints in the frontend.

# Optional: show the Admin role in the UI (advanced tuning tools).
# VITE_ENABLE_ADMIN_ROLE=1
